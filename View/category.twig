{% extends 'layout.twig' %}
{% import 'form.twig' as form %}


{% block content %}
    {% if app.session.user  %}
        <div class="jumbotron">
            <div class="container">
                <h1>{{ category.nom }}</h1>
                <p>{{ category.description }}</p>
            </div>
        </div>
    {% endif %}

    <div class="container">
        <h2>Liste des films liés à cette catégorie</h2>
    </div>
    <div class="container search_results">
        {# Tous les films liés à cette catégorie #}
    </div>
{% endblock %}

{% block javascripts %}

{% endblock %}